import 'package:flutter/material.dart';

class CicrularButton extends StatelessWidget {
  
  const CicrularButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Circular"),
    );
  }
}